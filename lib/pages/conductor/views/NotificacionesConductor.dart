import 'package:flutter/material.dart';

class NotificacionesConductor extends StatelessWidget {
  const NotificacionesConductor({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Vista: Notificaciones del Conductor"),
    );
  }
}
