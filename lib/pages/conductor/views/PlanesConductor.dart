import 'package:flutter/material.dart';

class PlanesConductor extends StatelessWidget {
  const PlanesConductor({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Vista: Planes / Membresía del Conductor"),
    );
  }
}
