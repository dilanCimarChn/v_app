import 'package:flutter/material.dart';

class HistorialConductor extends StatelessWidget {
  const HistorialConductor({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Vista: Historial del Conductor"),
    );
  }
}
