import 'package:flutter/material.dart';

class PagosCliente extends StatelessWidget {
  const PagosCliente({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text("Vista: Métodos de Pago"));
  }
}
