import 'package:flutter/material.dart';

class ComentariosCliente extends StatelessWidget {
  const ComentariosCliente({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text("Vista: Comentarios y Calificación"));
  }
}
